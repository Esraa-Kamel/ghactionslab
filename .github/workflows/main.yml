name: First WF

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest
    
    steps:
      - name: Print a welcome message
        run: echo "Welcome to our Github Actions WF"
      - name: Checkout Repo
        uses: actions/checkout@v6.1.0
    
    
  
